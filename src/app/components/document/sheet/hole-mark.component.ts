import { Component } from '@angular/core';

@Component({
  selector: 'app-hole-mark',
  imports: [],
  template: ``,
  styles: `
  :host {
    display: block;
      width: 10mm; 
      height: 0.25mm;
      background-color: var(--mat-sys-on-surface);
  }`,
})
export class HoleMarkComponent {}
